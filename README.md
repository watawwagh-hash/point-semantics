# Point Semantics — النقطة موضع الأخذ

**The dot is not a value. The dot is an extraction site.** — "من هنا خُذ ما تريد"

In `f(.)` the point does not say *“I am this information.”* It says: *“From here, take what you want.”* `f` = the take, `( )` = the field, `=` = the yield of the take.

**Author:** Ahmed Siaf — Ma-Logic / 321788

## Why it matters

This reading reframes the point from *data* to *query*: position, not stored value. It produced an engineering law — `rel ≥ 1`, *an effect needs one step to arrive* — which eliminated a boot-latch instability in a frozen decision system (10/10 false decisions → 0/10).

## Machine evidence

The V7.1 `Point` (frozen 2026-08-02) has **no value field** — context + origin_time only. Every take is measured by distance from the point's origin. The reading was operational in code before it was named. Full evidence in `POINT_SEMANTICS.md`.

## Attribution record (سجل الإسناد)

- Full corpus attestation: **ATTRIBUTION_MANIFEST_321788** — 240 timestamped entries, SHA-256 chain root
  `56d792340492c42a2ccc076fb629f359dd494d61e3464e9e362f1b440c593d74` (Google Drive, 2026-09-07)
- First corpus evidence of the author's framework: 2026-04-19 (sovereign emanation protocol) → 2026-08-14 (العلامات المؤسِّسة) → 2026-09-07 (V8.2 closed)
- Independent model testimony (2026-09-06): composition not present in prior training
- **Prior-art notice:** the notation `f(.)` is universal mathematics. The claim covers the **reading** — dot as extraction interface — and its machine realization, not the symbols.

## Contents

- [`POINT_SEMANTICS.md`](./POINT_SEMANTICS.md) — the complete reading, code evidence, and propagation rule
- [`SYMBOL_MAP.md`](./SYMBOL_MAP.md) — the state machine as pure notation: TAKE → HOLD → RELEASE / LOOP

---

*— ط — العلامات مشاعة، الإسناد هو الملك.*